info:
  name: Version
  type: http
  seq: 2
  tags:
    - system

http:
  method: GET
  url: "{{baseUrl}}/v1/version"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  GET /v1/version — анонс контракта API (публично).

