info:
  name: Delete Message
  type: http
  seq: 7
  tags:
    - messages
    - crud

http:
  method: DELETE
  url: http://localhost:9090/messages/:id
  params:
    - name: id
      value: "{{messageId}}"
      type: path
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("статус 204 No Content", function() {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Delete Message

  Удаляет сообщение по `:id` ({{messageId}}). Ожидается ответ 204 No Content.
  Запускайте последним в CRUD-флоу (seq 7).
